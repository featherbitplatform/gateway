---
title: error-page
description: Replace the body and content type of gateway-generated 404/500/502/503 responses with custom pages.
---

<span className="plugin-chip" style={{'--chip-color': '#dc2626'}}>error-page</span>

Replaces the body and `content-type` of gateway-generated error responses with configured pages. It is a response-phase node — place it after `upstream` (typically also downstream of the `error-handler` node whose output it decorates), before `client`.

Only four status codes are supported: **404, 500, 502, 503**.

## Configuration

All keys are optional; only the configured statuses are intercepted. Each of `error_404`, `error_500`, `error_502`, `error_503` is an object with:

| Key | Type | Default | Description |
|---|---|---|---|
| `body` | string | a built-in HTML page for that status (`<h1>502 Bad Gateway</h1>` ... with a featherbit footer) | The replacement response body. |
| `content_type` | string | `text/html` | The replacement `content-type` header value. |

Setting a key to an empty object `{}` selects the defaults for that status. A non-object `error_XXX` value, or a non-string `body`/`content_type`, fails at config load. `error_XXX` keys outside the supported set are ignored.

```yaml
type: error-page
config:
  error_502:
    body: '{"error": "upstream unavailable"}'
    content_type: application/json
  error_503: {}   # default featherbit HTML page
```

## Behavior

This plugin never fails at execution time — it always exits through the `success` port.

A response is rewritten only when **both** hold:

1. it was generated by a node that **failed** — the heuristic is `context.errors` being non-empty: a response produced after a failure (an upstream connection error, a failed IdP callout, the `error-handler` that rendered it) always carries the error record that routed it there, while a clean upstream response does not. An upstream's *own* 502 therefore passes through untouched;
2. its status code has a configured `error_XXX` page.

### Outcome exits are not replaced

Because the gate is the presence of an **error record**, this node does not touch responses that arrived on an [outcome port](../../concepts/policies-and-graphs.md#outcome-ports-and-the-mandatory-wiring-rule) — a `denied` 403, a `limited` 429, a `broken` 503, an `abort`. Those are deliberate, already-formed responses and carry no error record. In a normal graph they never reach this node anyway: an outcome port is wired straight to `client`.

To style a denial, throttle, or breaker response, route that port through a response-shaping node on its way to `client` — [`response-rewrite`](response-rewrite.md), or [`exit-transformer`](exit-transformer.md) with `always: true` — rather than expecting `error-page` to catch it.

The rewrite sets the body and `content-type`, and removes `content-length` and `content-encoding` (the server layer recomputes the length; the configured page is not encoded). The status code itself is never changed.

Behavior notes: the pages live directly in the node config — there is no separate metadata tier or `enable` flag; placing the node in the graph is the enable switch. Whether a response is gateway-generated is decided by the `context.errors` heuristic described above, which is narrower than APISIX's "source is not the upstream": deliberate rejections written by a plugin are outcomes here, not errors.

The plugin reads `context.errors` but never writes it, and does not touch `context.message`.
