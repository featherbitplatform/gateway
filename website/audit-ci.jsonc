{
  "$schema": "https://github.com/IBM/audit-ci/raw/main/docs/schema.json",
  // npm dependency CVE gate — fails on high/critical, reports the rest.
  // Same thresholds as the other scanners (see .github/workflows/security.yml
  // and dev/sast.ps1). Every allowlist entry needs a comment and a removal
  // condition — no silent suppression.
  "high": true,
  "allowlist": [
    // GHSA-w3rx-r6r6-pgpr + GHSA-5p2g-fcmc-qvqq (image-size infinite-loop DoS
    // on crafted ICNS/JXL/HEIF): no patched release exists (2.0.2 is latest
    // and flagged); pulled in via @docusaurus/mdx-loader, which only feeds it
    // images from this repo at build time — not attacker-reachable. Remove
    // once image-size ships a fix and docusaurus picks it up.
    "GHSA-w3rx-r6r6-pgpr",
    "GHSA-5p2g-fcmc-qvqq"
  ]
}
