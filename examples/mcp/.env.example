# Copy to .env (gitignored) and replace both values:
#   openssl rand -base64 32
# The read token can only inspect; the write token can change routes, policies,
# supernodes, plugin configs and stores on the running gateway.
FEATHERBIT_MCP_READ_TOKEN=replace-me-read
FEATHERBIT_MCP_WRITE_TOKEN=replace-me-write
